server {
    listen 8080;
    server_names localhost;

    root /var/www;

    location / {
        root /var/www;
        index index.html;
        autoindex on;
    }
    
    location /listing_on {
        autoindex on;
    }

    location /listing_off {
        autoindex off;
    }
}
