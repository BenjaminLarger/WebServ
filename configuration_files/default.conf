server {
    listen 8080;  # Listen on port #n
    server_name localhost;  # Specify the server name or domain

    location / {
        root /var/www/html;  # Root directory to serve files from
        index index.html;  # Default file to serve
    }
}